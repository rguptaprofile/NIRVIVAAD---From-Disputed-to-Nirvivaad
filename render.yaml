services:
  - type: web
    name: nirvivaad-api
    runtime: python
    buildCommand: python -m pip install --upgrade pip && python -m pip install --prefer-binary -r Backend/requirements.txt
    startCommand: uvicorn render_start:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB_NAME
        value: nirvivaad
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        sync: false
      - key: UPLOAD_DIR
        value: /tmp/nirvivaad-uploads
      - key: OPENAI_API_KEY
        sync: false
