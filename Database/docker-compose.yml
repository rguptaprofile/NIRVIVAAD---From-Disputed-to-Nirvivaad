services:
  mongo:
    image: mongo:8
    container_name: nirvivaad-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: nirvivaad
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
